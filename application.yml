spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
  application:
    name: microservice-dev
---
spring:
  profiles: test
  application:
    name: microservice-test

#  必须保存为UTF-8